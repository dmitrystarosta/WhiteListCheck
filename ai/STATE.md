# STATE.md

Журнал текущего состояния проекта. Обновлять после каждого завершённого этапа.
Последнее обновление: 24.08.2026 (v0.5.4 выпущена на GitHub и отправлена
на модерацию в RuStore: оператор мобильной сети в чипе и в «Поделиться»,
живая индикация проверки по каждому сайту, ненавязчивый триггер блока
отзыва, чистка экранов на Android TV, прокрутка онбординга и инструкции,
отключён системный бэкап + упрощён показ онбординга. v0.5.3 в RuStore уже
опубликована — v0.5.4 приедет после модерации).

## Текущая версия

- Опубликовано: **v0.5.4** (versionCode 16, versionName "0.5.4") — GitHub
  Releases (Latest); отправлена на модерацию в RuStore. Оператор мобильной
  сети в чипе и в «Поделиться», живая индикация проверки по каждому сайту,
  ненавязчивый триггер блока отзыва, чистка экранов на Android TV,
  прокрутка онбординга/инструкции, allowBackup="false" + упрощённый показ
  онбординга. Собрана и протестирована на Xiaomi 13T Pro, HTC U11, Android TV.
- Опубликовано: **v0.5.3** (versionCode 15, versionName "0.5.3") — GitHub
  Releases; **в RuStore опубликована** (прошла модерацию). Онбординг первого
  запуска, экран «Как остаться на связи», блок «нет рекламы» с кнопками
  отзыва (RuStore/GitHub), полировка подвала, чипа сети и шрифтов.
- Опубликовано: **v0.5.2** (versionCode 14, versionName "0.5.2") — GitHub
  Releases. Полировка: логотип-ссылка в подвале, стабильные
  раскрывашки на ТВ и после возврата в приложение, ссылка на сайт
  в «Поделиться».
- **RuStore: опубликована v0.5.3** (versionCode 15) — прошла модерацию.
  На модерацию отправлена **v0.5.4** (versionCode 16). versionCode должен
  быть больше опубликованного (15) — 16 подходит.
- (История магазина: v0.5.1 — первое обновление после 0.4.1, пользователям
  тогда приехало всё сразу: виджет, сохранение состояния, синхронизация;
  затем опубликована v0.5.3, вобравшая v0.5.2.)
- История: v0.5.1 (синхронизация виджета и экрана), v0.5 (виджет),
  v0.4.3 (стабильная навигация), v0.4.2
  (сохранение состояния), v0.4.1 (поделиться, тёмный ТВ, VPN),
  v0.4 (редизайн), v0.3.3–v0.3.1 (Android TV), v0.3 (настройка списков),
  v0.2.1 (первая с release-подписью), v0.2, v0.1.1, v0.1.
- Ставится поверх v0.2.1 и новее; поверх v0.2 и старше — нет (сменилась
  подпись), старую версию нужно удалить.

## Инфраструктура проекта (новое, 22.07.2026)

- **Сайт: https://belyjspisok.ru/** — лендинг приложения: разделы
  «Когда пригодится», «Как работает», виджет с легендой цветов,
  скриншоты интерфейса, Android TV, конфиденциальность, FAQ, кнопки
  RuStore/GitHub. Ссылка добавлена в README и в описание RuStore.
- **Почта проекта: belyjspisok@starosta.ru** — обновлена в PRIVACY.md,
  указана в README (раздел «Обратная связь») и в подвале сайта;
  в RuStore email обновляется владельцем.
- Репозиторий: https://github.com/dmitrystarosta/WhiteListCheck

## Состав v0.5.1 (что вошло)

Мелкий, но настоящий багфикс — сову на глобус не натягивали.

- **Синхронизация виджета и экрана.** Виджет и фоновый воркер пишут
  вердикт в SharedPreferences и перерисовывают виджет, но экран
  приложения (детальные карточки живут в памяти, в ScanState) об этом
  не знал. Баг ловится так: в приложении «всё в норме» → авиарежим →
  тап по виджету даёт «нет сети» (виджет сереет) → открываем приложение
  по иконке, а там всё ещё «всё в норме».
- **Решение:** в ScanState добавлено поле `checkedAt` (время получения
  показанного результата; пишется в обеих ветках runScan — и «нет сети»,
  и обычной — одним `now` вместе с last_check_ts). На событие ON_RESUME
  (DisposableEffect + LifecycleEventObserver в MainScreen) сверяем
  last_check_ts из prefs с state.checkedAt: если снаружи проверка
  НОВЕЕ И вердикт ОТЛИЧАЕТСЯ — молча вызываем runScan(). Совпадающий
  вердикт не пересканируем (лишний скан ни к чему).
- Побочный плюс: после фоновой смены режима открытие приложения тоже
  само подтягивает актуальный результат.
- **ВАЖНО:** LocalLifecycleOwner берётся из androidx.compose.ui.platform
  (в Compose 1.6.x / BOM 2024.06.00 ещё не deprecated; в 1.7+ переехал
  в androidx.lifecycle.compose — поправить импорт при обновлении BOM).

## Первые пользователи и отзывы (RuStore, 25–26.07.2026)

Приложение начали находить и ставить; первая обратная связь — тёплая.

- **Henry** (Xiaomi 25028RN03A, Android 15): «Очень хорошая идея, ничего
  лишнего, не бросайте проект». Поставил лайк. Уточнил, куда слать обратную
  связь — ответили: почта в RuStore или GitHub Issues. Владелец ответил
  на отзыв, упомянул, что виджет — идея пользователя.
- **Максим** (Nothing A059P, Android 16): «Очень полезное в наши дни
  приложение». Владелец поблагодарил, пригласил присылать идеи.
- Вывод: направление верное, «ничего лишнего» — ценят минимализм; стоит
  и дальше не раздувать функциональность. Обратную связь принимаем на
  belyjspisok@starosta.ru и в GitHub Issues.
- **Обновление (24.08.2026):** RuStore — рейтинг **4,4** (15 оценок),
  **1000+ загрузок**. Новые отзывы: Андрей («понятный интерфейс, всё по делу,
  ничего лишнего»), Владимир (комфортное меню), Рустам («без излишеств,
  рекламы и украшательств; удобный виджет, уведомления исправно»), Максим.
  Один негатив: Светлана «Ничего не работает» (1★) — ответили с просьбой
  уточнить, что именно (частый случай — VPN/нет сети, приложение отработало
  верно, но вердикт смутил). Минимализм подтверждают независимо несколько
  человек — курс верный.

## Состав v0.5.4 (что вошло)

Релиз «оператор + живая индикация». Плюс незапланированные фиксы, всплывшие
при тестах. Ядро проверки (параллельный `scanGroup`) НЕ трогали.

- **Оператор мобильной сети в чипе.** `TelephonyManager.getNetworkOperatorName()`
  — без опасных разрешений и БЕЗ правки манифеста (READ_PHONE_STATE не нужен).
  Новое поле `ScanState.operator`; читается ТОЛЬКО когда
  `net == "мобильный интернет"` (на Wi-Fi со вставленной SIM метод вернул бы
  имя → ложно вылезло бы в чипе). Вид: «Сеть: мобильный интернет · Tele2».
  **ВАЖНО:** оператор НЕ склеиваем в `networkType` — отдельное поле, иначе
  ломается `when(networkType)` (цвет и пояснение чипа).
- **Оператор в «Поделиться».** `shareVerdict` подставляет тот же формат
  «…· Tele2». Изначально забыли (был отдельный флажок «сайт не меняем»),
  дописали ДО публикации, чтобы совпадало с чипом и текстом релиза.
- **Живая индикация проверки по каждому сайту.** `ProbeResult` += поле
  `checking: Boolean = false` (дефолт → старые позиционные конструкторы не
  ломаются). `Scanner.check()` сделан public. В `runScan` при старте все сайты
  засеиваются `checking=true` (карточки групп показываются сразу), затем все
  пробы запускаются разом (параллельно, как было), и каждая по завершении
  обновляет СВОЮ строку в `state` → крутилка (`CircularProgressIndicator` 16dp)
  сменяется галочкой/крестиком и «120 мс»/ошибкой. В сводке группы во время
  скана — «проверка…». **ВАЖНО:** обновления строк идут на Main-диспетчере
  (`scope = rememberCoroutineScope`), read-modify-write `state` синхронный
  (без suspend внутри) → параллельные апдейты не теряются. `scanGroup`
  оставлен для ФОНОВОЙ проверки/виджета (там поштучная индикация не нужна).
- **Триггер блока отзыва — ненавязчиво.** Счётчик `scan_count` в prefs растёт
  ТОЛЬКО на пользовательские проверки на главной (кнопка «Проверить»):
  фон/виджет счётчик не трогают, тихая пересинхронизация ON_RESUME вызывает
  `runScan(countsForReview = false)`. Блок на главной виден в окне
  `scan_count in 3..5`; с 6-й скрывается сам (если так и не кликнули); клик по
  RuStore/GitHub ставит `review_dismissed` и прячет сразу. Футер перечитывает
  счётчик через привязку к `state.checkedAt` (scanTick), иначе Compose мог бы
  пропустить рекомпозицию.
- **Блок отзыва добавлен и на экран «Как остаться на связи»** (`ReviewCard`),
  но ТОЛЬКО когда экран открыт из подвала (`showBack`) и НЕ на ТВ; отступ 28dp
  отделяет его от инструкции. Клик по кнопке там тоже ставит `review_dismissed`.
- **Android TV: убраны экраны под телефон.** Онбординг не показывается
  (`isTv = FEATURE_LEANBACK`), блок отзыва не показывается нигде (ни на главной,
  ни в инструкции) — оставить отзыв с пульта неудобно (нет браузера/клавиатуры).
- **Показ онбординга упрощён — ЗАМЕНЯЕТ подход v0.5.3.** Убрана хрупкая
  проверка `firstInstallTime == lastUpdateTime`: на части прошивок (HTC/HTC-подобных,
  MIUI-реинсталл) она давала `false` даже на чистой установке, а очистка данных
  её не сбрасывала → онбординг не появлялся. Теперь: `android:allowBackup="false"`
  в манифесте (prefs реально стираются при удалении/очистке, не восстанавливаются
  системным бэкапом) + гейт по флагу `onboarded` (+ `!isTv`). Детерминированно:
  чистая установка / очистка данных → онбординг; обновление поверх → нет.
- **Прокрутка длинных экранов.** `OnboardingWelcome`, второй экран онбординга и
  «Как остаться на связи» переведены на `weight(1f) + verticalScroll` с
  закреплёнными СНИЗУ кнопками («Включить» / «Готово»). На «Как остаться на связи»
  из подвала заголовок со стрелкой закреплён СВЕРХУ тем же паттерном, что
  «Списки сайтов» (стрелка `offset -12dp` + `titleLarge` Bold) — общий стиль.
  Причина: на маленьких экранах (HTC U11) и при крупном системном шрифте текст
  упирался в кнопки.
- **Проверено на Xiaomi 13T Pro, HTC U11, Android TV:** прокрутка, крутилки,
  триггер отзыва (появление после 3 / скрытие после 6 / скрытие по клику),
  отсутствие онбординга и блока отзыва на ТВ, стабильный онбординг на чистой
  установке, оператор в чипе на мобильной сети и в «Поделиться» — ОК.

## Состав v0.5.3 (что вошло)

Релиз «первое впечатление и удержание»: онбординг, помощь с фоном, отзыв.
Ядро проверки не трогали — только новые экраны и полировка.

- **Онбординг первого запуска** (OnboardingFlow → OnboardingWelcome +
  ConnectivityHelpScreen). Экран 1: приветствие (логотип-марка по центру +
  название) + ненавязчивое предложение «Фоновая проверка и уведомления»
  («Включить» / «Позже»). «Включить» включает фон (bg_enabled +
  scheduleBackground) и ведёт на экран 2; «Позже» — сразу на главный.
  Экран 2 «Как остаться на связи»: кнопка «Разрешить» (POST_NOTIFICATIONS)
  со статусом «✓ Разрешено» + кнопка «Открыть настройки» (батарея/автозапуск).
- **Разрешение запрашивается ТОЛЬКО на экране 2.** Раньше экран 1 сам
  вызывал системный диалог, а экран 2 проверял статус до ответа → висела
  «Разрешить», хотя разрешение уже дано. Исправлено: один источник запроса.
- **Показ онбординга — по времени установки, НЕ по SharedPreferences.**
  freshInstall = (firstInstallTime == lastUpdateTime) + флаг onboarded.
  Причина: системный бэкап Android при переустановке восстанавливает prefs
  (bg_enabled/last_verdict), и проверка «свежести» по prefs ложно пропускала
  онбординг. Обновившимся (не чистая установка) он не показывается.
  Проверено на MIUI: чистая установка → онбординг; «Позже» ведёт на главный;
  после «Разрешить» статус меняется на «Разрешено».
- **Экран «Как остаться на связи» переоткрывается из подвала** (та же
  ConnectivityHelpScreen, showBack=true). Кнопка батареи/автозапуска —
  ACTION_APPLICATION_DETAILS_SETTINGS (fallback ACTION_SETTINGS), БЕЗ
  спец-разрешения (Play не придерётся, манифест не меняли — POST_NOTIFICATIONS
  уже был). На всех прошивках ведёт на страницу приложения; на MIUI там есть
  «Автозапуск» и «Батарея», на стоковом Android — только «Батарея».
  Автозапуск MIUI прочитать нельзя (нет API) → кнопка-действие, а не статус
  «Настроено»; батарею читать можно (PowerManager.isIgnoringBatteryOptimizations)
  — отложено до отзывов.
- **Блок «в приложении нет рекламы» в подвале** (ReviewCard): текст + две
  одинаковые по форме кнопки-логотипа. RuStore (res/drawable/ic_rustore.xml,
  официальная синяя иконка из брендбука, Image без тонировки) → RUSTORE_URL;
  GitHub (ic_github.xml, Octocat, Icon с tint по теме) → REPO_URL (звезда).
  Перекрашивать бренды в один цвет нельзя (гайдлайны) — единообразие через
  одинаковые контейнеры кнопок.
- **Полировка подвала:** отступ над карточкой отзыва (отделяет футер от
  технической части, ~22dp), равные отступы вокруг логотипа, компактные
  строки «© 2026 · Dmitry Starosta / Версия · проверить обновления / Как
  остаться на связи ⓘ»; «?» заменён значком ⓘ (Icons.Filled.Info,
  единообразно с чипом и сноской).
- **Чип «Сеть» центрирован** по вертикали относительно кнопки «поделиться»
  (Box .align(CenterVertically)) и выровнен между вердиктом и «Проверить»
  (спейсер 12/12).
- **Крупнее шрифты** на онбординге и в инструкции (описания bodyLarge,
  заголовки карточек titleMedium); «Разрешено» — bodyMedium жирным.
- Golos Text уже был подключён — онбординг наследует шрифт автоматически.
- **Идея на будущее (в TODO):** редизайн онбординга — меньше текста,
  больше графики (скриншоты/иллюстрации), «люди не любят читать».

## Состав v0.5.2 (что вошло)

Полировочный релиз: одна видимая мелочь (логотип) и два багфикса из
живого использования на ТВ и телефоне, плюс смена ссылки в «Поделиться».

- **Логотип-знак в подвале + ссылка на сайт.** Над копирайтом по центру —
  векторный знак «уши+глобус» БЕЗ плитки-фона, нарисован в Canvas
  (composable `AppLogoMark`): круги ушей и глобуса заливаются цветом ФОНА
  экрана (поэтому нижние дуги ушей корректно перекрываются глобусом, видны
  только линии), линии — цветом `primary` из темы. Знак адаптивный:
  коричневые линии на светлой теме, светло-бежевые на тёмной. Тап открывает
  belyjspisok.ru (константа `SITE_URL`). Размер 52dp (видимый знак ~40dp +
  зона нажатия). Отдельного XML-ассета нет (в отличие от иконки-плитки,
  которую пробовали ранее и отклонили). На ТВ знак стал фокусируемым —
  в подвале теперь ДВЕ цели: логотип → сайт, «Версия…» → страница релизов.
  Проверено: телефон (светлая и тёмная), ТВ (тёмная).

- **Стабильные раскрывашки (фикс сворачивания).** Флаги «развёрнуто» чипа
  «Сеть», сноски «Почему ошибки разные» и трёх карточек групп ПОДНЯТЫ на
  уровень MainScreen (`netExpanded`, `whyExpanded`, `expA`/`expB`/`expC`),
  а не хранятся внутри элементов LazyColumn. Причина: уехавший за нижний
  край элемент LazyColumn уничтожается вместе с состоянием, и rememberSaveable
  внутри него восстанавливался неодинаково — на ТВ блок сворачивался сразу
  при прокрутке за экран, на телефоне слетал после выхода-возврата в
  приложение. Чип сети «держался» лишь потому, что он первый элемент списка
  и почти не уезжает. Композаблы NetworkChip / GroupCard / Footnote теперь
  получают состояние снаружи (`expanded` + `onToggle`). netExpanded
  по-прежнему ключуется типом сети (при смене сети сворачивается).
  ВАЖНО: не возвращать эти флаги внутрь композаблов. Граница: заход в
  «Списки сайтов» и возврат по-прежнему сбрасывает раскрывашки (MainScreen
  покидает композицию) — это не регресс; если понадобится, поднять флаги
  до уровня App. Проверено: ТВ-прокрутка и выход-возврат сохраняют состояние.

- **Ссылка в «Поделиться» → сайт.** В тексте кнопки «Поделиться вердиктом»
  вместо длинной ссылки на RuStore теперь `SITE_URL` (belyjspisok.ru):
  сайт и объясняет приложение, и ведёт на все магазины. Константа
  RUSTORE_URL оставлена в коде (нигде не используется, но пусть лежит).

## Состав v0.5 (что вошло) — ВИДЖЕТ-ИНДИКАТОР

Виджет на рабочем столе (запрос пользователя Рустама из RuStore; дизайн
согласован: уши логотипа ярким цветом состояния + сфера тем же цветом
приглушённо).

- Файлы: 5 векторов res/drawable/widget_logo_{normal,whitelist,vpn,
  nonet,neutral}.xml (геометрия 1:1 из ic_launcher); res/drawable/
  widget_bg.xml; res/layout/widget_status.xml (ТОЛЬКО RemoteViews-
  совместимые view: LinearLayout / ImageView / TextView / ProgressBar);
  res/xml/widget_info.xml (1×1, resizable, updatePeriodMillis 30 мин);
  receiver StatusWidget в манифесте; в MainActivity.kt —
  StatusWidgetUpdater + StatusWidget + WidgetScanWorker + хуки.
- Цвет значка = последний вердикт: норма 66BB6A/4A6B4C, белый список
  EF5350/7C4340, VPN 42A5F5/3E5A78, нет сети 455A64/37474F,
  нейтрально 6D4C41 (не проверялось / UNKNOWN).
- Данные: last_verdict + last_check_ts (пишут ручная, фоновая и
  виджет-проверки). Обновление виджета: после каждой проверки,
  системный тик 30 мин, onResume приложения.
- **ПОДПИСЬ ВРЕМЕНИ — АБСОЛЮТНАЯ** («9:15» через DateFormat "H:mm"),
  а НЕ относительная «N мин назад». Почему: RemoteViews — статический
  снимок, относительный счётчик сам не «тикает», ему нужна перерисовка
  каждую минуту, а MIUI такие фоновые обновления душит → счётчик вечно
  висел на «сейчас». Абсолютные часы верны без единого обновления.
  До первой проверки — «нажмите». НЕ возвращать относительный счётчик.
- Устаревание: >60 мин → setImageAlpha 130 + янтарное время #FFD180.
- **ТАП ПО ВИДЖЕТУ = ПРОВЕРКА** (не открытие приложения): широковещалка
  самому себе StatusWidget.ACTION_WIDGET_SCAN → onReceive →
  showChecking() (крутящийся ProgressBar на месте подписи; свой drawable
  в RemoteViews анимировать нельзя, только штатный indeterminate) +
  OneTimeWork WidgetScanWorker БЕЗ сетевого constraint (иначе при
  отсутствии сети спиннер крутился бы вечно — воркер сам быстро вернёт
  «нет сети»). WidgetScanWorker уведомлений НЕ шлёт (пользователь и так
  смотрит на виджет) и всегда пишет результат + время + обновляет
  виджет, включая «нет сети».
- **ФИКС «открывалось сброшенным»:** PendingIntent уведомления теперь
  через packageManager.getLaunchIntentForPackage (ACTION_MAIN/LAUNCHER +
  RESET_TASK_IF_NEEDED) — поднимает существующую задачу как иконка,
  результаты сохраняются. Прямой Intent(MainActivity) этого не давал.
- ГРАБЛИ: при создании файлов через GitHub web путь вводить БЕЗ
  markdown-звёздочек (папка «**layout**» ломала сборку).

## Решённые вопросы дизайна (не переоткрывать без причины)

- **Двухзонный тап по виджету — РЕШЕНО: НЕ ДЕЛАЕМ** (22.07.2026).
  Была идея: верх (логотип) открывает приложение, низ (время) запускает
  проверку. Отказались: на 1×1 подпись времени — крохотная мишень, зоны
  ничем не обозначены (скрытый UI), «тап по времени = открыть» —
  неинтуитивно, а открытие и так есть по иконке рядом. Решение владельца:
  «пусть сперва привыкнут к 1×1». Вернуться к идее имеет смысл только
  на увеличенном виджете (2×1 и больше), и тогда большую зону логичнее
  отдать проверке, мелкую — открытию.

## Состав v0.4.3 / v0.4.2 / v0.4.1 (кратко)

- **v0.4.3:** ScanState и CoroutineScope сканирования подняты на уровень
  App — при переходе в «Списки сайтов» MainScreen покидает композицию,
  и remember-состояние уничтожалось вместе с результатами, а scope
  отменялся (проверка обрывалась, running=true оставался навсегда →
  вечное «Проверяю…»). **ВАЖНО: state и scope НЕ возвращать внутрь
  MainScreen — оба подъёма намеренные.**
- **v0.4.2:** rememberSaveable вместо remember в трёх раскрывашках
  (чип сети, карточки групп, «Почему ошибки разные») — LazyColumn
  уничтожает уехавшие за экран элементы вместе с remember;
  AndroidManifest configChanges="uiMode|orientation|screenSize" —
  смена темы и поворот больше не пересоздают Activity.
- **v0.4.1:** кнопка «поделиться вердиктом» (справа от чипа сети,
  Icons.Outlined.Share, offset −1dp, текст со ссылкой на RuStore);
  ТВ всегда в тёмной теме (isAppDark = FEATURE_LEANBACK || система,
  на неё переведены ВСЕ цветовые помощники); VPN проверяется ПЕРВЫМ
  в networkType (с Android 12 VPN-сеть сообщает и нижележащий
  транспорт), без сканирования приложений и GeoIP; чип сети скрыт
  во время проверки; выравнивания offset-сдвигами; копирайт выше
  ссылки версии (ТВ-прокрутка следует за фокусом).

## Что уже реализовано (накопительно)

- Ручная проверка трёх групп сайтов, вердикты 4 типов + UNKNOWN
- Виджет-индикатор: цвет = вердикт, тап = проверка, абсолютное время,
  спиннер на время проверки, тускнение при устаревании (v0.5)
- Синхронизация виджета и экрана при возврате в приложение (v0.5.1)
- Логотип-знак в подвале со ссылкой на сайт, адаптивный по теме (v0.5.2)
- Раскрывашки (чип сети, «Почему ошибки», группы) переживают прокрутку
  и возврат в приложение на телефоне и ТВ (v0.5.2)
- Ссылка в «Поделиться» ведёт на сайт belyjspisok.ru (v0.5.2)
- Онбординг первого запуска + экран «Как остаться на связи» (v0.5.3)
- Блок «в приложении нет рекламы» с кнопками отзыва RuStore/GitHub (v0.5.3);
  показ по триггеру (окно 3..5 проверок, скрытие после 6 или по клику),
  добавлен и на экран «Как остаться на связи» (v0.5.4)
- Оператор мобильной сети в чипе и в «Поделиться» (v0.5.4)
- Живая индикация проверки: крутилка у каждого сайта → галочка/крестик (v0.5.4)
- Онбординг и блок отзыва не показываются на Android TV (v0.5.4)
- allowBackup="false" + показ онбординга по флагу onboarded (v0.5.4)
- Прокрутка онбординга и «Как остаться на связи» с закреплёнными кнопками (v0.5.4)
- Кнопка «поделиться вердиктом» (v0.4.1)
- Тёмная тема на ТВ всегда; на телефоне — по системной настройке (v0.4.1)
- Подсказка при VPN-туннеле на устройстве (v0.4.1)
- Редизайн (v0.4): две темы, Golos Text, закреплённые шапка и вердикт,
  чип сети с пояснениями, сворачиваемые карточки групп
- Настройка списков (v0.3): SharedPreferences custom_lists, приоритет
  над REMOTE_CONFIG_URL, фон использует те же списки
- Android TV (v0.3.1–v0.3.3): баннер, LEANBACK_LAUNCHER, подсветка
  фокуса D-pad, TRANSPORT_ETHERNET
- Фоновая проверка (WorkManager, 15 мин) с уведомлениями о смене режима
- Кликабельные названия сайтов, русские описания ошибок
- CI: подписанный release-APK, имя WhiteListCheck_v<версия>.apk
- Сайт belyjspisok.ru, README с кнопками и скриншотом виджета,
  PRIVACY.md, LICENSE (MIT), FONT_LICENSE.txt (OFL)

## Списки сайтов (встроенные, MainActivity.kt)

- Группа А (белый список): Яндекс, ВКонтакте, Госуслуги, Mail.ru
- Группа Б «Обычный интернет (вне списка)»: Habr, 4PDA, Google, Википедия
- Группа В (заблокированные, контроль): Instagram*, X, Rutracker
- REMOTE_CONFIG_URL пуст; пользователь может менять списки в приложении
- Решение от 08.07.2026: Drive2 и Banki.ru исключены из Б (открывались
  при белом списке — перечень Минцифры пополняется и различается
  у операторов)

## Что протестировано

- **Android TV, Ethernet с реальным кабелем (22.07.2026): ПРОВЕРЕНО.**
  Чип показывает «Сеть: Ethernet (кабель) ⓘ», по тапу — пояснение
  «Это домашний интернет, белые списки обычно действуют на мобильном.
  Результат показывает состояние именно кабельного подключения.»
- Телефон Xiaomi 13T Pro, v0.5/v0.5.1: тап по виджету запускает
  проверку, спиннер сменяется временем; авиарежим → тап по виджету →
  «нет сети» (серый); открытие приложения после этого подтягивает
  «нет сети» на экран (вердикты совпадают). Проверено «не глубоко» —
  редкие баги возможны, всплывут со временем.
- Телефон, v0.4.1: «поделиться», скрытие чипа при проверке,
  «Сеть: VPN ⓘ» при туннеле, светлая тема, выравнивания — работают.
- Android TV, v0.4.1: тёмная тема принудительно, прокрутка доезжает
  до копирайта.
- Полевой кейс (17.07, старый HTC): TLS-ошибка vk.com из-за устаревших
  сертификатов — не блокировка; вердикт по большинству отработал верно.

НЕ протестировано: виджет на ТВ-лаунчере; живое push-уведомление при
смене режима; поведение при реальном включении белого списка.

## Известные проблемы и особенности

- **VPN на роутере не определяется** — системное ограничение Android
  (для устройства это обычный Wi-Fi), НЕ баг. Вердикт при этом всё
  равно покажет «похоже, включён VPN» по доступности сайтов.
- **ТВ: кнопка сброса списков прилипает к нижнему краю экрана** —
  прокрутка на ТВ следует за фокусом. Косметика; чистого решения без
  экспериментальных API нет. Отложено.
- Версии до 0.3.1 невидимы в лаунчере Android TV — удалять через
  Настройки → Приложения → Показать все приложения.
- MIUI может убивать фоновый воркер без ручных разрешений (Автозапуск +
  снятие ограничений батареи). С v0.5.3 есть внутренняя подсказка «Как
  остаться на связи» с переходом в настройки приложения, но тумблеры
  включает сам пользователь (статус автозапуска MIUI прочитать нельзя).
- Интервал фоновой проверки нестрогий (15+ мин).
- На старых устройствах возможны TLS-ошибки отдельных сайтов
  (сертификаты) — пояснение в сноске.
- Инцидент безопасности (закрыт): первый release-ключ скомпрометирован,
  перевыпущен 06.07.2026.

## Текущая задача — ЖДЁМ

- **RuStore: v0.5.4 отправлена на модерацию** (поверх опубликованной v0.5.3).
  Дождаться публикации. При/после публикации: обновить скриншоты карточки на
  актуальный дизайн (теперь с оператором в чипе и живой индикацией), проверить
  email (belyjspisok@starosta.ru), ссылку на сайт.
- **Отвечать на отзывы RuStore** и собирать из них сигнал (пока: ценят
  минимализм, «ничего лишнего»). Первые два отзыва обработаны.
- **4PDA:** тема создана 17.07.2026, на премодерации. Следить за QMS.
  После публикации — написать модератору о статусе автора темы,
  отвечать, собирать отчёты из регионов.
- **RuMarket, GetApps:** подана v0.3.1 (~11.07) — проверить статусы,
  при одобрении обновить до актуальной версии.
- Категория Android TV в RuStore (после публикации).

## Роадмап (после v0.5.4)

Инкрементально, «версии как маркетинг»: внятный список «Что нового»
в каждой, доводим каждую фичу до ума. Детали в TODO.md. Идею у конкурента
(ЧекНет) берём, но дизайн любого экрана — свой, с нуля.

- **v0.5.4 — оператор + живая индикация. ✅ СДЕЛАНО** (см. «Состав v0.5.4»).
  Оператор в чипе — готовый кирпич для строк лога.
- **v0.6.0 — логи.** Локальный лог проверок (ручных/фоновых/виджет) +
  экран «История»: цветные строки по вердикту (схемы виджета), разделители
  дней логотипом-маркой (AppLogoMark), кружок-вход рядом с «Поделиться»
  (Icons.AutoMirrored.Outlined.List), закреплённая СНИЗУ кнопка «Скачать
  лог (CSV)» — не в конце списка (иначе скроллить ~672 строки за неделю),
  список новыми сверху. CSV (для рус. Excel «;»). Обновить PRIVACY.md.
- **v0.7.0 — аналитика.** Тепловые карты (по часам/дням) + сводка за
  неделю поверх лога, когда накопятся данные. Дизайн рисуем ЗАНОВО, не
  копируя раскладку ЧекНета.
- **Доработка вердикта UNKNOWN** (группа А лежит, Б работает): понятный
  текст, отдельный вердикт «российские сайты недоступны», совет держать
  в А несколько разных сайтов. Детали в TODO.md.
- Собирать обратную связь (4PDA, RuStore, знакомые) прежде, чем городить
  новые функции.

## Идеи на будущее

- **Строчка в сноске «Почему ошибки разные ⓘ»** — что для заблокированных
  в РФ сайтов TLS-ошибка/сброс соединения это нормально (штатный признак
  блокировки по SNI через ТСПУ, а не сбой). Владелец решил добавить в
  следующих версиях, не в 0.5.4. Контекст (24.08.2026): Instagram в группе В
  падает с «ошибка шифрования (TLS)» — ожидаемо, контрольная группа так и
  должна себя вести; способ обрыва (RST/таймаут/DNS) варьируется по
  операторам и регионам, но для контроля любой неуспех = «недоступен».
- **Редизайн онбординга** — меньше текста, больше графики (скриншоты
  уведомления, иллюстрации): «люди не любят читать». Нужен отдельный
  дизайн; отложено.
- Удалённое обновление списков (REMOTE_CONFIG_URL, хостинг из белого
  списка).
- Кириллические домены (punycode).
- Ручной переключатель темы (💤 по запросу пользователей).
- Увеличенный виджет 2×1+ (и только тогда — двухзонный тап).
- Переименовать APK в старых релизах v0.1–v0.2.1.
- Письмо блогеру VladimirKoPSS (черновик согласован).
- Google Play (отложено); верификация разработчиков Google (~2027).
