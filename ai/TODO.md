# TODO.md

Список задач проекта WhiteListCheck. Статусы: ✅ сделано · 🔄 в работе ·
⬜ не начато · 💤 отложено.

## Выпущено

**v0.2.1 (08.07.2026):** release-подпись из CI; контрольные сайты
группы Б заменены (Drive2, Banki.ru → Google, Википедия).

**v0.3 (10.07.2026):** экран настройки списков (SharedPreferences,
диалоги подтверждения, сброс); фоновая проверка использует
пользовательские списки; автоименование APK (шаг Rename в build.yml;
archivesBaseName в Gradle не работает — не повторять).

**v0.3.1–v0.3.3 (11–12.07.2026), Android TV:** баннер, LEANBACK_LAUNCHER,
uses-feature required=false; подсветка фокуса D-pad (tvFocusHighlight;
onFocusChanged ДО clickable); TRANSPORT_ETHERNET и «Ethernet (кабель)».

**v0.4 (14.07.2026), редизайн:** светлая + тёмная темы по системе;
Golos Text (res/font, OFL); закреплённые шапка и вердикт + единая
прокрутка; чип «Сеть: … ⓘ»; сворачиваемые карточки групп; значки
статуса Check/Close; расшифровка ошибок за ⓘ. versionCode 8.

**v0.4.1 (19.07.2026):** кнопка «поделиться вердиктом»; ТВ всегда
в тёмной теме (isAppDark: FEATURE_LEANBACK || система); подсказка при
VPN (TRANSPORT_VPN проверяется ПЕРВЫМ); чип сети скрыт во время
проверки; выравнивания offset-сдвигами; копирайт выше ссылки версии.
versionCode 9.

**v0.4.2 (19.07.2026):** rememberSaveable вместо remember в трёх
раскрывашках; configChanges="uiMode|orientation|screenSize" — смена
темы и поворот не пересоздают Activity. versionCode 10.

**v0.4.3 (20.07.2026):** ScanState и CoroutineScope сканирования
подняты на уровень App (результаты и проверка переживают переход
в «Списки сайтов»). ВАЖНО: не возвращать их внутрь MainScreen.
versionCode 11.

**v0.5 (20.07.2026):** ВИДЖЕТ-индикатор на рабочий стол (запрос
Рустама). Цвет значка = вердикт; тап по виджету запускает проверку
в фоне (ACTION_WIDGET_SCAN → onReceive → showChecking() со спиннером
ProgressBar + OneTimeWork WidgetScanWorker БЕЗ сетевого constraint);
подпись — АБСОЛЮТНОЕ время «9:15» (DateFormat "H:mm"), а не «N мин»
(RemoteViews — статический снимок, MIUI душит updatePeriodMillis →
висело «сейчас»); тускнение >60 мин; фикс «открывалось сброшенным» —
PendingIntent уведомления через getLaunchIntentForPackage.
versionCode 12.

**v0.5.1 (22.07.2026):** синхронизация виджета и экрана. Поле
ScanState.checkedAt + сверка на ON_RESUME (LifecycleEventObserver):
если снаружи (виджет/фон) проверка новее И вердикт другой — молча
перезапускаем runScan(). Раньше после проверки виджетом в авиарежиме
приложение по иконке показывало старое «всё в норме». ВАЖНО:
LocalLifecycleOwner из androidx.compose.ui.platform (в Compose 1.7+
переехал в androidx.lifecycle.compose — поправить при обновлении BOM).
versionCode 13.

**v0.5.2 (26.07.2026):** полировка. (1) Логотип-знак в подвале —
векторный «уши+глобус» без плитки (Canvas `AppLogoMark`), адаптивный
по теме (коричневые линии на светлой, светлые на тёмной), тап открывает
belyjspisok.ru (`SITE_URL`); отдельного XML-ассета нет. (2) Стабильные
раскрывашки — флаги «развёрнуто» чипа «Сеть», сноски «Почему ошибки»
и карточек групп подняты на уровень MainScreen (netExpanded/whyExpanded/
expA/expB/expC); раньше жили внутри элементов LazyColumn и терялись при
уходе за экран (на ТВ сворачивались при прокрутке, на телефоне после
выхода-возврата). НЕ возвращать флаги внутрь композаблов. (3) Ссылка
в «Поделиться» — вместо RuStore теперь сайт belyjspisok.ru. versionCode 14.

**v0.5.4 (24.08.2026):** «оператор + живая индикация». (1) Оператор
мобильной сети в чипе (TelephonyManager.getNetworkOperatorName(), без
разрешений; поле ScanState.operator, только для мобильной сети) — «Сеть:
мобильный интернет · Tele2»; тот же формат в «Поделиться». (2) Живая
индикация: ProbeResult += checking; Scanner.check() public; runScan засеивает
сайты checking=true и обновляет строки по мере ответа (крутилка → галочка/
крестик), проверка осталась параллельной; scanGroup оставлен для фона. (3)
Триггер блока отзыва: scan_count только на пользовательские проверки главной
(фон/виджет/тихая ON_RESUME-пересинхронизация не считаются), окно 3..5,
скрытие после 6 или по клику RuStore/GitHub (review_dismissed); блок добавлен
и на «Как остаться на связи» (из подвала, не на ТВ). (4) Android TV: убраны
онбординг и блок отзыва. (5) allowBackup="false" + показ онбординга по флагу
onboarded (убрана хрупкая проверка firstInstallTime==lastUpdateTime, ломалась
на части прошивок). (6) Прокрутка OnboardingWelcome / 2-го экрана / «Как
остаться на связи» с закреплёнными кнопками; шапка со стрелкой закреплена
сверху как в «Списках сайтов». versionCode 16.

**v0.5.3 (15.08.2026):** «первое впечатление и удержание». (1) Онбординг
первого запуска (OnboardingFlow → OnboardingWelcome + ConnectivityHelpScreen):
приветствие + предложение фоновой проверки; экран «Как остаться на связи»
(разрешение уведомлений + переход в настройки батареи/автозапуска). Показ
только чистой установке — по firstInstallTime==lastUpdateTime + флаг
onboarded (бэкап Android восстанавливает prefs, поэтому НЕ по ним).
Разрешение запрашивается только на 2-м экране (иначе статус не совпадал).
(2) Экран «Как остаться на связи» переоткрывается из подвала; кнопка —
ACTION_APPLICATION_DETAILS_SETTINGS без спец-разрешения (манифест не меняли).
(3) Блок «нет рекламы» в подвале (ReviewCard) с кнопками-логотипами RuStore
(ic_rustore.xml) и GitHub (ic_github.xml, tint по теме). (4) Полировка:
крупнее шрифты онбординга/инструкции, «Разрешено» жирным, отступы подвала,
центрирование чипа сети, «?» → значок ⓘ. versionCode 15.

**v0.5.5 (05.09.2026):** багфикс «проверка больше не зависает без
интернета». Приложение навсегда застревало в «Сканирую…», если связь есть,
а интернета нет (авария у провайдера, роутер без WAN): быстрый путь
«нет сети» не срабатывает (сеть формально есть), а DNS-резолвинг не покрыт
таймаутами HttpURLConnection и может ждать вечно; блокирующий сокет
корутиной не отменяется → coroutineScope ждал пробы бесконечно. Решение:
PROBE_TIMEOUT_MS=10 000 и SCAN_TIMEOUT_MS=25 000; check() стал обёрткой над
rawCheck(), проба запускается в ОТДЕЛЬНОМ probeScope (SupervisorJob+IO,
НЕ дочерний вызывающему) и ожидается через await() — иначе структурная
конкурентность дождалась бы блокирующего ребёнка и потолок не сработал бы;
в runScan добавлена «добивка» незавершённых строк («нет ответа (таймаут)»).
Побочно чинит зависание CheckWorker/WidgetScanWorker. НЕЛЬЗЯ использовать
NET_CAPABILITY_VALIDATED как признак «нет сети» — при белом списке сеть
невалидирована. versionCode 17.

## Сейчас в работе — ЖДЁМ ОТВЕТА

- ✅ **RuStore: v0.5.4 опубликована** (прошла модерацию).
- 🔄 **RuStore: v0.5.5 отправлена на модерацию** (05.09.2026, поверх
  опубликованной v0.5.4, versionCode 17). Через console.rustore.ru.
  После публикации:
  - обновить скриншоты карточки на актуальный дизайн (теперь оператор в
    чипе + живая индикация); старые скриншоты устарели;
  - убедиться, что email в карточке — belyjspisok@starosta.ru;
  - добавить в карусель карточку с виджетом (card6_widget), если ещё нет;
  - ссылка на сайт belyjspisok.ru в карточке;
  - категория Android TV в RuStore.
- 🔄 **Отвечать на отзывы RuStore.** Первые два (Henry, Максим,
  25–26.07) — положительные, обработаны. Ценят минимализм («ничего
  лишнего») — держать это в уме, не раздувать функциональность.
- 🔄 **4PDA:** тема создана 17.07.2026, на премодерации. Следить за QMS.
  После публикации — написать модератору о статусе автора темы,
  отвечать в теме, собирать отчёты из регионов.
- 🔄 **RuMarket, GetApps:** подана v0.3.1 (~11.07) — проверить статусы,
  при одобрении обновить до актуальной версии.
- 🔄 **F-Droid: MR !46918 в очереди на мёрж** (review-requested, воспроизводимо,
  с подписью автора). 05.09.2026 рецепт синхронизирован с v0.5.5: `commit: v0.5.5`
  (имя тега, не хэш), versionCode 17, CurrentVersion 0.5.5, changelog'и 17.txt
  в ru-RU и en-US. Настоящий пайплайн запускает linsui — ждём. Детали: ai/FDROID.md.

## Роадмап (после v0.5.5)

Инкрементально, «версии как маркетинг». Идею у конкурента (ЧекНет)
берём, но дизайн любого экрана — свой, с нуля.

- ✅ **v0.5.4 — оператор + живая индикация.** СДЕЛАНО (24.08.2026).
  Оператор в чипе — готовый кирпич для строк лога.
- ✅ **v0.5.5 — потолки ожидания (багфикс).** СДЕЛАНО (05.09.2026).
  Внеплановый релиз: зависание при «связь есть, интернета нет».
- ⬜ **v0.6.0 — логи + «История».** Записывать итог каждой проверки
  (ручной/фоновой/виджет) с пометкой источника. Экран «История»:
  цветные строки по вердикту (схемы виджета: синий=VPN, зелёный=норма,
  красный/янтарь=белый список, серый=нет сети), разделители дней
  логотипом-маркой (AppLogoMark), кружок-вход рядом с «Поделиться»
  (Icons.AutoMirrored.Outlined.List), закреплённая СНИЗУ кнопка «Скачать
  лог (CSV)» — не в конце списка (иначе до неё скроллить ~672 строки
  за неделю), список новыми сверху. Формат CSV (для рус. Excel «;»),
  через системное «Поделиться». Обновить PRIVACY.md: лог локальный,
  отправляется только вручную. Польза: отчёты из регионов, диагностика
  убийства воркера MIUI (дырки в логе), разбор UNKNOWN.
- ⬜ **v0.7.0 — аналитика/паттерны.** Поверх лога: сводка за неделю
  (сколько ограничений / всего / дольше всего), «когда обычно
  ограничивают», тепловые карты по часам и дням. Оживает только на
  накопленных данных. Дизайн рисуем ЗАНОВО, не копируя раскладку ЧекНета.
- ⬜ **Доработать вердикт UNKNOWN «непонятная ситуация».** Возникает,
  когда группа А (белый список) недоступна, а группа Б работает.
  Причины: (а) за границей/через VPN, а в А остались только сайты,
  не открывающиеся с зарубежных IP; (б) сбой российских сайтов при
  работающих зарубежных. Придумать понятный текст, возможно отдельный
  вердикт «российские сайты недоступны, остальное работает», совет
  держать в А несколько РАЗНЫХ сайтов.
- ⬜ **Строчка в сноске «Почему ошибки разные ⓘ»:** для заблокированных
  в РФ сайтов TLS-ошибка/сброс — норма (блокировка по SNI через ТСПУ,
  не сбой). Владелец решил добавить в следующих версиях, не в 0.5.4.
- ⬜ **Мелочи из анализа конкурента (по желанию):** раздельные тумблеры
  уведомлений (о начале / о восстановлении), настраиваемый интервал
  проверки, плитка в шторке (TileService). Не срочно.
- ⬜ Собрать обратную связь по виджету, онбордингу и кнопке «поделиться»
  (4PDA, RuStore, знакомые).

## Продвижение

- ⬜ Рустаму (запросил виджет) — написать после публикации v0.5.1
  в RuStore.
- ⬜ Письмо блогеру VladimirKoPSS (черновик согласован в чате).
- ⬜ Пост в Telegram-каналы.
- ⬜ Попросить пользователей из других регионов проверить актуальную
  версию и прислать результат (кнопка «поделиться» это упрощает).
- ⬜ Сайт belyjspisok.ru — по мере надобности дополнять (раздел
  «Что нового»? ссылки на релизы?).

## Решено — НЕ делаем

- ❌ **Двухзонный тап по виджету** (верх — открыть приложение, низ —
  проверка). Решение от 22.07.2026: на 1×1 подпись времени слишком
  мелкая мишень, зоны ничем не обозначены (скрытый UI), «тап по времени
  = открыть» неинтуитивно, а открытие и так есть по иконке рядом.
  «Пусть сперва привыкнут к 1×1». Вернуться имеет смысл только
  на увеличенном виджете 2×1+, и тогда большую зону отдать проверке.

## Известные мелочи (не блокируют)

- 💤 ТВ: кнопка сброса списков прилипает к нижнему краю экрана
  (прокрутка следует за фокусом); чистого решения без экспериментальных
  API нет.
- 💤 VPN на роутере не определяется чипом сети — системное ограничение
  Android, вердикт при этом всё равно отработает по доступности сайтов.
- 💤 Виджет на ТВ-лаунчере не тестировался (на ТВ виджеты обычно
  не используются — низкий приоритет).

## Долгосрочные идеи

- 💤 **Редизайн онбординга** — меньше текста, больше графики (скриншоты
  уведомления, иллюстрации). «Люди не любят читать». Нужен отдельный дизайн.
- 💤 Увеличенный виджет 2×1 и больше (и только тогда — двухзонный тап).
- 💤 Удалённое обновление списков (REMOTE_CONFIG_URL, хостинг
  из белого списка).
- 💤 Кириллические домены в настройках (punycode).
- 💤 Ручной переключатель темы (по запросу пользователей).
- 💤 Переименовать APK в старых релизах v0.1–v0.2.1.
- 💤 Google Play: $25, зарубежная карта, 12 тестировщиков × 14 дней, AAB.
- 💤 Верификация Android-разработчиков Google (~2027), бесплатный
  уровень на 20 устройств.
